<?xml version="1.0" encoding="utf-8"?>
<Window x:ConnectionId='1'
    x:Class="F_Port.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:F_Port"
    xmlns:models="using:F_Port.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="F-Port App Store">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="20" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <TextBlock Text="F-Port App Store" Style="{StaticResource TitleTextBlockStyle}" FontSize="28"/>
            <TextBlock Text="Discover and install apps" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>

        <!-- Content -->
        <Grid Grid.Row="1" Padding="20">
            <ProgressRing x:ConnectionId='2' x:Name="LoadingRing" Width="50" Height="50" IsActive="False"/>
            
            <GridView x:ConnectionId='3' x:Name="AppsGridView" 
                                                  
                      SelectionMode="None"
                      IsItemClickEnabled="True"
                                                        >
                <GridView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                                CornerRadius="8" 
                                Padding="16" 
                                Width="200" 
                                Height="280"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1">
                            <StackPanel Spacing="8">
                                <Image Source="{Binding IconUrl}" 
                                       Width="80" 
                                       Height="80" 
                                       HorizontalAlignment="Center"
                                       Stretch="Uniform"/>
                                <TextBlock Text="{Binding Name}" 
                                           Style="{StaticResource SubtitleTextBlockStyle}" 
                                           TextWrapping="Wrap" 
                                           TextTrimming="CharacterEllipsis"
                                           MaxLines="2"/>
                                <TextBlock Text="{Binding Developer}" 
                                           Style="{StaticResource CaptionTextBlockStyle}" 
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Text="{Binding Category}" 
                                           Style="{StaticResource CaptionTextBlockStyle}" 
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xE734;" Foreground="Gold"/>
                                    <TextBlock Text="{Binding Rating}" 
                                               Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                                <Button x:ConnectionId='6' Content="{Binding InstallButtonText}" 
                                                                   
                                        Tag="{Binding Id}"
                                        HorizontalAlignment="Stretch"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>

            <TextBlock x:ConnectionId='4' x:Name="ErrorTextBlock" 
                       Text="Failed to load apps. Please check your internet connection." 
                       Visibility="Collapsed"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Foreground="Red"/>
        </Grid>
    </Grid>
</Window>

